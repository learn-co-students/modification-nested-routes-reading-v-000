<h2><%= @post.title %></h2>
<p>by <%= link_to @post.author.name, author_path(@post.author) if @post.author %> (<%= link_to "Edit Post", edit_post_path(@post.author, @post) %>)</p>
<p><%= @post.description %> </p>
